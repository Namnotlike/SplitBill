@model int

<a class="nav-link position-relative" asp-page="/Notifications/Index">
    🔔
    @if (Model > 0)
    {
        <span class="badge rounded-pill bg-danger" style="font-size: 0.65rem; vertical-align: top; margin-left: -4px;">@(Model > 99 ? "99+" : Model.ToString())</span>
    }
</a>
